{
  "name": "scrapstack-api",
  "version": "1.0.0",
  "description": "",
  "main": "api.js",
  "directories": {},
  "scripts": {
    "build": "esbuild source/api.ts --bundle --minify --platform=node --outdir=out",
    "deploy": "npm run build && cd ../terraform && terraform apply -var-file dev_variables.tfvars",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Scapsters",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.1",
    "mongodb": "^6.19.0"
  },
  "devDependencies": {
    "@types/mongodb": "^4.0.7",
    "@types/node": "^24.3.0",
    "esbuild": "^0.24"
  }
}
