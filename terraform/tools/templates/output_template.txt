output "${lambda_name}_gateway_endpoint" {
  description = "${http_method} request to this to call the ${lambda_name} endpoint"
  value       = module.gateway.api_gateway_${lambda_name}_endpoint_url
}